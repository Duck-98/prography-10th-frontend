export type ConsentStepType = {
  consent?: boolean;
};

export type PersonalInfoStepType = {
  name?: string;
  email?: string;
  phone?: string;
};

export type PositionStepType = {
  position: string;
};
